설계 패턴 정리

도메인 모델 패턴
- 비즈니스 로직을 엔티티, 서비스와 같은 도메인 객체에 담아 처리하는 방식입니다.
- 이번 투두 프로젝트에서는 `TodoService`, `TodoEntity` 등을 통해 해당 패턴을 적용했습니다.

트랜잭션 스크립트 패턴
- 비즈니스 로직을 한 곳의 함수에 순차적으로 나열하여 처리하는 절차형 방식입니다.
- 규모가 작은 CRUD 프로젝트에서는 빠르게 개발하기 위해 종종 사용됩니다.

